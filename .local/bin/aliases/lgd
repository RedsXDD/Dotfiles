#!/usr/bin/env sh

if ! which lazygit >/dev/null 2>&1; then
	echo 'ERROR: lazygit not installed!'
	exit 1
fi

lazygit -p "$HOME/.local/sources/Reds/dotfiles"

# vim:fileencoding=utf-8:foldmethod=marker
