#!/usr/bin/env sh

case "$1" in
 -r|r)
  list="$(ls -R $1 | grep -E '\.(jpg|jpeg|png|bmp|gif|tiff|heif|raw|psd)$')"
 ;;
esac

#wall_path="$@"
# Change Wallpaper & Color Scheme:
#wal -a 75 -i "$wall_path"
#wal -o "$HOME"/.config/wal/postrun -a 75 -i "$wall_path"
#sleep 0.3
#wall_file="$(cat "$HOME/.cache/wal/wal")"
#feh --bg-scale "$wall_file"

#nitrogen --set-scaled "$(< "${HOME}/.cache/wal/wal")" &&
#echo "[xin_-1]" > "$HOME"/.config/nitrogen/bg-saved.cfg &&
#echo "file=$wall_file" >> "$HOME/.config/nitrogen/bg-saved.cfg" &&
#echo 'mode=0' >> "$HOME/.config/nitrogen/bg-saved.cfg" &&
#echo 'bgcolor=#000000' >> "$HOME/.config/nitrogen/bg-saved.cfg" &&

