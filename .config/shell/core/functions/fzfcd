# vim:fileencoding=utf-8:foldmethod=marker:filetype=sh

fzfcd(){
	choice="$(find "$HOME" -maxdepth 4 -type d -not -path "$HOME" | fzf-tmux -p 75% -w 75% --preview 'ls -lA {}' --prompt '> Change directory > ')"
	[ -n "$choice" ] && cd "$choice"
}
